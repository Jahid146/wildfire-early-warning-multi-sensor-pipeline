__pycache__/
*.pyc
*.pyo

# Local pipeline outputs from running the scripts yourself -- not committed,
# since these are regenerated by running the pipeline (real data is private;
# synthetic data can be regenerated with generate_synthetic_data.py).
data/raw/
data/processed/
data/results/

# Keep the small shipped example untouched
!data/synthetic_sample/
